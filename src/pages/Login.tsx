import LoginCard from "../componenets/auth/LoginCard";

const Login = () => {
  return (
    <div className="min-h-screen bg-gray-100 flex items-center justify-center">
      <LoginCard />
    </div>
  );
};

export default Login;